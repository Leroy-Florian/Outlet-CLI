# docs/

- `playground-mockup.html` — maquette du playground interactif (à rapatrier ; le playground
  lui-même est post-v1, il consommera `@outlet/hateoas` et `@outlet/effect-react`).
- Les conventions du projet vivent dans `CLAUDE.md` (racine) et
  `.specify/memory/constitution.md`.
